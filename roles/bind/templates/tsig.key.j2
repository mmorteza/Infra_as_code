// Managed by Ansible - do not edit by hand.
// Shared secret used to authenticate zone transfers between master + slave.

key "{{ bind_tsig_key_name }}" {
    algorithm {{ bind_tsig_algorithm }};
    secret "{{ bind_tsig_secret }}";
};
